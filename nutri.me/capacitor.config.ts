import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.saudebemestar.app',
  appName: 'saude e bem estar',
  webDir: 'build'
};

export default config;
